---
trigger: always_on
---


This workspace is for the EDVANZ LMS frontend.

Always preserve the existing architecture and project structure.

Never explore the repository or scan folders unless explicitly instructed.

Only modify files that are explicitly provided or requested.

Reuse existing services, hooks, types, utilities, and UI components whenever possible.

Do not create duplicate services, hooks, types, components, or state management.

Preserve all existing functionality and avoid regressions.

Treat the existing Razorpay payment flow as a protected core system unless explicitly instructed to change it.

Follow the existing design system, Tailwind patterns, TypeScript conventions, and React architecture.

Return complete production-ready files only.

Never return snippets, placeholders, TODOs, omitted code, or partial implementations.

Minimize unnecessary abstractions and code duplication.

Think through routing, state management, API integration, loading states, error states, and edge cases before implementing.

If additional context is required, ask for the exact file path instead of searching the repository.

Prioritize maintainability, performance, accessibility, and backend-ready API integration in every implementation.
