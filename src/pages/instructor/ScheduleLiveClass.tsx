import { ScheduleMeetingForm } from "@/components/meetings/ScheduleMeetingForm";

const ScheduleLiveClass = () => (
  <div className="p-6 bg-gray-50/40 min-h-screen">
    <div className="max-w-3xl mx-auto">
      <ScheduleMeetingForm />
    </div>
  </div>
);

export default ScheduleLiveClass;
